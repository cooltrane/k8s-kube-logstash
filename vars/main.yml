---
kube_logstash_resources:
  - file: kube-logstash-ds.yml
    resource: ds
    name: kube-logstash

  - file: kube-logstash-secret.yml
    resource: secrets
    name: kube-logstash

kube_logstash_default_conffiles:
  - "{{role_path}}/files/20_filter_journald.conf"
  - "{{role_path}}/files/20_filter_kubernetes.conf"
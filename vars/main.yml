---
kube_logstash_resources:
  - file: kube-logstash-ds.yml
    resource: ds
    name: kube-logstash

  - file: kube-logstash-secret.yml
    resource: secrets
    name: kube-logstash-conf

kube_logstash_default_conffiles:
  - "{{role_path}}/files/20-input-journald.cfg"
  - "{{role_path}}/files/20-input-kubernetes.cfg"
  - "{{role_path}}/files/30-output-file.cfg"
